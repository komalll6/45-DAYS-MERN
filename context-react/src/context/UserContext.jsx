//create context

//STEPS :->
//1. create context
//2. provide context
//3. use context

import { createContext} from "react" //inbuild function in react

const UserContext = createContext()  //UserContext => ANY NAME , createContext() => MUST ENSURE THE SMALL AND CAPITAL LETTER

export default UserContext 